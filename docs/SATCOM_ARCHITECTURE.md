# SATCOM architecture and MCP runbook

Updated September 7, 2026. The publisher confirmed `satcom.conews.press` as the target for this task. The original `satcom.copress.press` does not resolve; no DNS changes were required.

## Structure and ownership

| Layer | Source / route | Meaning |
|---|---|---|
| Dashboard | `idigitalpro1/copress-dashboard`, Vercel `5280menu/copress-dashboard`, `/` | Existing static HTML/CSS/JavaScript operator interface |
| Development hub | `/codex`, generated by `scripts/build-codex.mjs` | Public structure, priorities, prompt downloads/copy and MCP connection |
| Public context | `data/codex/context.json` and three reviewed `.md` prompts | Explicit allowlist; no private repository mirroring |
| MCP runtime | `api/mcp.js`, `lib/satcom-mcp.js`, `/mcp` rewrite | Official MCP SDK; stateless Streamable HTTP with JSON responses |
| Service health | `/api/health` | Identifies this context service; explicitly does not check upstream systems |
| Canonical operations | Private `idigitalpro1/codex` | Cross-system decisions, top ten priorities, business plan, release evidence |
| Existing vault | `/apistore`, browser-local storage | Separate from public MCP; no credential retrieval through this service |
| Existing Hermes proxy | `/console/api/hermes/*` → `https://codex.conews.press/api/v1/platform/hermes/*` | Preserved; upstream connectivity is not asserted by this release |
| Local tools | Ace / Hermes / voice / local vault | Must verify machine identity, authentication and actual capability separately |
| Subscriber alias | `subscribe.thevillager.today` root → `/subscribe-villager/` | Preserved redirect; do not repurpose or break printed destinations |

The local checkout also contained an unmerged receptionist MCP draft and campaign work. This release starts from remote main and does not merge or activate those drafts. Public MCP has no send, deployment, secret, arbitrary-fetch, subscriber or telephony capability.

## MCP contract

Endpoint: `https://satcom.conews.press/mcp`. The direct `/api/mcp` handler exposes the same service. Authentication is not required because every response is reviewed public information already published under `/data/codex/`. This is not an authentication bypass for another backend. Any future private or mutating capability needs a separately scoped, authenticated design.

| Feature | Name | Scope |
|---|---|---|
| Tool | `satcom_structure` | Dated structure, route map and boundaries |
| Tool | `satcom_priorities` | Ten public priorities, owner, target window, next action; Partners primary |
| Tool | `satcom_prompt` | Enum: `grok-development`, `satcom-release`, `partners-editorial` |
| Resource | `satcom://context` | Curated public JSON |
| Prompts | Same three names | User-selectable reviewed prompts; no dispatch |

All tools advertise read-only, non-destructive, idempotent and closed-world annotations. Input cannot select a filesystem path or URL. Browser Origins are allowlisted; native clients without Origin are supported. Requests require JSON and the MCP Accept headers. A GET to `/mcp` intentionally returns 405 (POST supported, no standalone SSE stream); use `/api/health` for a simple service response. Responses are not cached. Neither health nor public context asserts live provider connectivity.

## Connect and verify

```sh
codex mcp add satcom --url https://satcom.conews.press/mcp
npm ci
npm run build
npm test
npm run verify:mcp -- https://satcom.conews.press/mcp
```

Restart/reload a client that does not refresh its configured MCP list. The verification script uses the official SDK client: initialize, tools/list, three read-only tool calls, resources/list + read, prompts/list + three prompt reads. Configuration alone is not proof of connection.

For local development, `npm run dev` serves `http://127.0.0.1:4321`; run `npm run verify:mcp` against that default. Browser health checks are limited to this service. Review `/codex` at desktop/mobile widths, keyboard navigation, copy feedback and prompt overflow. `npm test` verifies real SDK requests, parallel clients, input rejection, allowed capabilities, health semantics and preserved routing. `npm run build` refreshes the committed static page from the same reviewed content used by MCP; rebuild after any prompt/context edit.

## Release and future work

Keep the existing Vercel project and aliases. Deploy a preview, verify the exact artifact, record rollback, and promote only within the user's release authorization. Check the subscriber alias after release. Store exact commits, deployment IDs and verification results in the private canonical operations release record.

Next upgrades should make existing operating cards explicit about unavailable data and last successful provider checks. The legacy root dashboard includes other browser-local/sample behavior; this release does not claim that all of it is live. `conews.press/codex` is a separate origin/path and remains unresolved by this release. The shared-spine and reference-publication gates still precede town expansion.

Official implementation references: [MCP SDK server guide](https://ts.sdk.modelcontextprotocol.io/server) and [Streamable HTTP transport specification](https://modelcontextprotocol.io/specification/2025-11-25/basic/transports).

## Development Kanban follow-up

Version 1.2.0 adds `/kanban`, `/api/development-board` and the fourth read-only tool `satcom_board`. Forty-one reviewed items were created in the existing `https://admin.conews.press/console/api/kanban` store and reread successfully. `data/codex/development-board.json` contains the reviewed public definitions, backend IDs and dated release snapshot. Only allowlisted IDs appear publicly; unrelated backend cards are excluded. No mutation proxy was added.

Live title, owner, priority and column come from the backend. Acceptance/source notes remain the reviewed definitions. A failed, invalid or incomplete upstream response displays an explicit dated snapshot rather than claiming a fresh empty board. Use Admin Kanban for card moves; a previously blocked item moved to Active leaves the Blocked lane. Adding new public items requires adding their reviewed definitions/IDs and deploying. Private tasks remain in the private source-of-truth repository.

The existing admin handler's authentication and durable/concurrent storage require a separate hardening item: its route source can fall back to a local JSON file, and this task's card POST did not require credentials. Do not represent this as a hardened private task backend. Keep cards here free of secrets or personal/customer information.

Read `data/codex/token-efficient-development.md` for compact task packets and bounded context. API keys are configured directly in the intended client, never in board cards or public prompts.
