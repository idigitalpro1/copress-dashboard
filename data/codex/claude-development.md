# Claude — primary fallback

GPT-6 Astra in Codex is primary. Fallback order is Claude, Grok Bot, Cursor, Hermes 2.0. Do not delegate or spawn additional agents. Direct model prompts are the current working method; SATCOM mission control is still under development.

Read https://satcom.conews.press/codex and one relevant card at https://satcom.conews.press/kanban. Use the public read-only MCP https://satcom.conews.press/mcp: satcom_structure for routing and satcom_board with a project filter and small limit. Follow the Astra suggestions recorded in the card. Preserve completed work and carry the current evidence, next action, acceptance check and blocker forward. Confirm the prior executor stopped before taking over; a timeout alone does not prove this. Do not automatically replay uncertain writes.

The publisher is on a remote laptop without server access. Localhost is that laptop, not Ace or the Hermes server. Public HTTPS context access does not establish a remote shell. Verify the intended host, Hermes version and model before server work. If unavailable, report the exact blocker and prepare the prompt for later; do not substitute another machine.

GUI development for news, activity and town sites is as important as content. Implement only the current publisher-authorized scope with small diffs and focused meaningful checks. The PDF-to-WordPress request remains audit-only until its refactoring sequence is approved. Do not create or modify pipeline files or automate the CMS UI in that audit.

Partners in the Community is the primary advertising and sponsorship platform. Preserve editorial provenance, actual dates/bylines, reader URLs and unique original/licensed photos or generated, inspected, persisted and labeled illustrations. Do not expose secrets, subscriber records or unapproved sponsor claims. Do not send campaigns or change financial settings from a reusable prompt.

Record a concise result and evidence on the same task card. The public MCP is read-only, so use the existing authorized Kanban editor for a status change, or return a ready-to-paste update. Report prepared, connected and verified execution distinctly.
